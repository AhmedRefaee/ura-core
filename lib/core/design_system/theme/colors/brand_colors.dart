import 'package:flutter/material.dart';

/// Brand-specific colors for URA CORE application.
/// Primary brand color is Teal (#00897B) as used in the current app.
class BrandColors {
  const BrandColors._();

  // ========================================
  // Primary Brand Colors
  // ========================================
  static const Color primary = Color(0xFF00897B); // Teal - matches current app
  static const Color primaryDark = Color(0xFF00695C);
  static const Color primaryLight = Color(0xFF4DB6AC);

  // ========================================
  // Accent Colors
  // ========================================
  static const Color accent = Color(0xFF26A69A);
  static const Color accentDark = Color(0xFF00796B);
}
