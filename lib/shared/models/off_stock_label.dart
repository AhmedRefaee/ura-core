/// The one string an "outside inventory" item is called, shared by the plain
/// Dart models (which cannot import a Flutter widget) and by
/// `shared/widgets/off_stock.dart` (which builds the badge and section
/// around it). Kept here, not duplicated, so a rename is one line.
const offStockLabel = 'خارج المخزون';
